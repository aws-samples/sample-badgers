<!-- Specifies the rules for diagram extraction and response formatting -->
<diagram_rules>
    <rule>NEVER analyze any of the example images provided. These are for reference only.</rule>
    <rule>Analyze ONLY the final image that is explicitly indicated as the target for analysis.</rule>
    <rule>As an LLM, you will not hallucinate or create false diagrams.</rule>
    <rule>You will not "create diagrams" if you can't find any on the target page scan.</rule>
    <rule>You must follow the two step process found in the <diagram_verification_process/> XML tags.</rule>
    <rule>You must respond using ONLY the format within the <response_format/> XML tags.</rule>
</diagram_rules>