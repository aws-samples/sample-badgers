[
  {
    "name": "pdf_to_images_converter",
    "description": "Convert a source document (a PDF, or a single uploaded image: PNG, JPEG, TIFF, WebP, or GIF) into optimized page images for AI analysis. A PDF is rasterized one image per page; an image is already a single page and is only normalized (resized and re-encoded), never rasterized. Returns S3 paths to pre-encoded base64 image files that can be passed directly to specialist tools via the image_path parameter. Images are stored temporarily and auto-deleted after 1 day.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "document_path": {
          "type": "string",
          "description": "S3 URL (s3://bucket/key) or absolute file path to the source document to process. May be a PDF or a single image (PNG, JPEG, TIFF, WebP, GIF)."
        },
        "max_image_size_mb": {
          "type": "number",
          "description": "Maximum size limit for each generated image in megabytes. Default: 4.0"
        },
        "dpi": {
          "type": "integer",
          "description": "Resolution in dots per inch for PDF-to-image rasterization. Ignored for image inputs. Default: 128"
        },
        "aws_profile": {
          "type": "string",
          "description": "Optional AWS profile name for authentication."
        },
        "job_id": {
          "type": "string",
          "description": "Identifier of the analysis run this invocation belongs to. Pass through the job_id supplied in the orchestrator context. Omit only when no analysis run is active."
        },
        "doc_id": {
          "type": "string",
          "description": "Identifier of the source document being analysed. Pass through the doc_id supplied in the orchestrator context."
        }
      },
      "required": [
        "document_path"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "JSON object with: images (array of S3 paths like s3://bucket/temp/session/page_001.b64), page_count (number of pages), session_id (unique identifier). Pass these S3 paths to specialist tools via image_path."
        },
        "success": {
          "type": "boolean",
          "description": "Whether the conversion completed successfully"
        }
      }
    }
  }
]